function Functions() {
	// document.addEventListener(`DOMContentLoaded`, () => {



	const addForm = document.querySelector(`.new_task_input`);
	const addBtn = document.querySelector(`.adding_btn`)

	let itemsArr = [];


		class ItemObg {
			constructor(done, content, urgent, important) {
				this.done = done;
				this.content = content;
				this.urgent = urgent;
				this.important = important;
			}
		}
	
		// addBtn.addEventListener(`click`, (e) => {
			
			// e.preventDefault();


		// })

	// })
}

export default Functions